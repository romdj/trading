'use strict';

export const main = async (event, context) => {
  if (!event.Records || event.Records.length === 0) {
  }
  try {
  //   storedObject = await downloadFile(event);
  //   const transactionId = storedObject.metadata['transactionid'];
  //   const hsdpId = storedObject.metadata['hsdpid'];

  //   dbsLogger = await DbsLogger.initialize(event, context, transactionId, hsdpId);
  // } catch (err) {
  //   return { status: ResponseStatus.ERROR };
  }
};

