{
  "name": "tickermonitor",
  "version": "0.3.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "run-history": "node --experimental-modules dataFetcher.mjs",
    "version": "conventional-changelog -p angular -i CHANGELOG.md -s && git add CHANGELOG.md",
    "version-bump": "npm version minor; conventional-changelog-cli",
    "check-deps": "snyk test",
    "test": "npm run check-deps && echo \"Error: no test written yet! (Don't forget TDD!)\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.491.0",
    "object-hash": "^1.3.1"
  },
  "devDependencies": {
    "conventional-changelog-cli": "^2.0.21",
    "elasticsearch": "^15.5.0",
    "mongoose": "^5.5.10",
    "request": "^2.88.0",
    "request-promise": "^4.2.4",
    "request-promise-native": "^1.0.7",
    "snyk": "^1.192.6",
    "uuid": "^3.3.2",
    "yahoo-finance": "^0.3.6"
  }
}
